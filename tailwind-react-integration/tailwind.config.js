module.exports = {
  purge: [
    "./src/**/*.{js,jsx,ts,tsx}", // This ensures Tailwind scans all your JavaScript/TypeScript/JSX/TSX files
    "./public/index.html", // Make sure to scan the HTML file
  ],
  darkMode: false, // You can also set this to 'media' or 'class' if you're using dark mode
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
module.exports = {
  purge: [
    "./src/**/*.{js,jsx,ts,tsx}", // This ensures Tailwind scans all your JavaScript/TypeScript/JSX/TSX files
    "./public/index.html", // Make sure to scan the HTML file
  ],
  darkMode: false, // You can also set this to 'media' or 'class' if you're using dark mode
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
